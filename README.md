## 📂 1. ส่วนหลังบ้าน (Backend Server)
**ไฟล์:** `index.js`

ทำหน้าที่เป็น **API Server** คอยรับคำสั่งจากหน้าเว็บและคุยกับฐานข้อมูล MySQL

### ⚙️ การตั้งค่าระบบ (Configuration)
* **Libraries:**
    * `express`: ใช้สร้าง Web Server และจัดการ Route (URL)
    * `cors`: ปลดล็อกความปลอดภัย ให้หน้าเว็บสามารถส่งข้อมูลหา Server ได้โดยไม่ติด Permission
    * `mysql2`: ไดรเวอร์สำหรับเชื่อมต่อกับ Database
* **Database Connection:** สร้างการเชื่อมต่อโดยใช้ Host `localhost` และ Database ชื่อ `testdb`

### 🔗 API Endpoints (จุดเชื่อมต่อข้อมูล)

#### 1. จัดการข้อมูลนักวิ่ง (CRUD)
* **`GET /api/runners` (ดึงข้อมูล)**
    * **การทำงาน:** สั่ง SQL `SELECT * ... ORDER BY id DESC`
    * **เพื่อ:** ดึงรายชื่อนักวิ่งทั้งหมดออกมา โดยเรียงจากคนที่สมัคร "ล่าสุด" ขึ้นก่อน
* **`POST /api/runners` (บันทึกข้อมูล)**
    * **การทำงาน:** รับค่า JSON `{name, distance, price}` จากหน้าเว็บ -> สั่ง SQL `INSERT INTO ...`
    * **เพื่อ:** บันทึกผู้สมัครคนใหม่ลงตาราง
* **`PUT /api/runners/:id` (แก้ไขข้อมูล)**
    * **การทำงาน:** รับค่า `id` จาก URL และ `name` ใหม่จาก Body -> สั่ง SQL `UPDATE ... WHERE id=?`
    * **เพื่อ:** แก้ไขชื่อนักวิ่ง โดยเจาะจงเฉพาะแถวที่มี ID ตรงกัน
* **`DELETE /api/runners/:id` (ลบข้อมูล)**
    * **การทำงาน:** รับค่า `id` จาก URL -> สั่ง SQL `DELETE FROM ... WHERE id=?`
    * **เพื่อ:** ลบข้อมูลนักวิ่งออกจากระบบถาวร

#### 2. ส่วนรายงานผล (Reporting)
* **`GET /api/report/total` (ยอดรวม)**
    * **การทำงาน:** ใช้ฟังก์ชัน SQL Aggregate `SUM(price)` เพื่อรวมเงิน และ `COUNT(*)` เพื่อนับจำนวนคน
    * **เพื่อ:** ส่งค่าตัวเลขสรุป 2 ตัวไปโชว์ที่ Dashboard
* **`GET /api/report/breakdown` (แยกประเภท)**
    * **การทำงาน:** ใช้คำสั่ง `GROUP BY distance`
    * **เพื่อ:** สั่งให้ Database จัดกลุ่มข้อมูลตามระยะทาง (5KM, 10KM, 21KM) แล้วนับยอดของแต่ละกลุ่มแยกกัน

---

## 💻 2. ส่วนหน้าจอลงทะเบียน (Frontend UI)
**ไฟล์:** `run_project.html`

ทำหน้าที่เป็นหน้าจอหลักสำหรับผู้ใช้งาน (User Interface)

* **Framework:** ใช้ **Bootstrap 5** ในการจัด Layout แบบ Grid System และ Card เพื่อความสวยงาม
* **ส่วนฟอร์ม (Form):**
    * **Dropdown (`<select>`):** ใช้สำหรับเลือกระยะทาง เพื่อป้องกันผู้ใช้กรอกข้อมูลผิดรูปแบบ
    * **Readonly Input:** ช่อง "ค่าสมัคร" ถูกล็อคไว้ (`readonly`) ผู้ใช้ไม่สามารถพิมพ์เองได้ ต้องให้ระบบคำนวณเท่านั้น
* **ส่วนตาราง (Table):**
    * มี `<tbody id="runnerTable">` ที่ว่างเปล่า เพื่อรอรับข้อมูลจาก JavaScript มาสร้างแถวตาราง

---

## 🧠 3. ส่วนควบคุมการทำงาน (Frontend Logic)
**ไฟล์:** `run_script.js`

ทำหน้าที่เป็น "สมอง" ของหน้าลงทะเบียน เชื่อมโยง HTML เข้ากับ Backend

### 🔄 ฟังก์ชันหลัก (Key Functions)

1.  **Event Listener (Calculation Logic)**
    * **การทำงาน:** ดักจับเหตุการณ์ `change` ที่ Dropdown ระยะทาง
    * **Logic:**
        * ถ้าเลือก 5KM -> ตั้งราคา 500
        * ถ้าเลือก 10KM -> ตั้งราคา 700
        * ถ้าเลือก 21KM -> ตั้งราคา 900
    * **ผลลัพธ์:** ราคาจะเด้งขึ้นมาอัตโนมัติทันทีที่เลือก

2.  **`loadRunners()` (Read)**
    * **การทำงาน:** ใช้ `fetch()` ไปขอข้อมูลจาก API
    * **ผลลัพธ์:** วนลูป (Loop) สร้าง HTML `<tr>` เพื่อแสดงรายชื่อนักวิ่งในตาราง

3.  **`addRunner()` (Insert)**
    * **การทำงาน:**
        1.  ดึงค่าจากช่องกรอก (Validation: ถ้าว่างให้แจ้งเตือน)
        2.  ห่อข้อมูลเป็น JSON
        3.  ส่งไป Server ด้วย Method `POST`
    * **ผลลัพธ์:** ข้อมูลถูกบันทึก และตารางจะรีเฟรชใหม่

4.  **`updateRunner(id)` (Update)**
    * **การทำงาน:** ดึงค่าชื่อที่ถูกแก้ไขใน Input -> ส่งไป Server ด้วย Method `PUT` พร้อม `id`

5.  **`deleteRunner(id)` (Delete)**
    * **การทำงาน:** แสดง Popup `confirm` ยืนยัน -> ส่งคำสั่ง `DELETE` ไปยัง Server ตาม `id` นั้นๆ

---

## 📊 4. ส่วนหน้าจอรายงาน (Dashboard)
**ไฟล์:** `report.html` และ `report_script.js`

แยกออกมาเป็นหน้าเฉพาะสำหรับดูภาพรวมข้อมูล (แยก View กับ Edit ออกจากกัน)

* **การแสดงผล:** แบ่งเป็น 2 ส่วน
    1.  **Big Number Cards:** แสดงยอดเงินรวมและจำนวนคนทั้งหมด (ตัวเลขใหญ่ๆ ดูง่าย)
    2.  **Breakdown Table:** ตารางแจกแจงรายละเอียดว่าแต่ละระยะทางมีคนสมัครกี่คน
* **Logic การทำงาน:**
    * เรียก API `/api/report/total` เพื่อเอายอดรวมมาใส่การ์ด
    * เรียก API `/api/report/breakdown` เพื่อเอาข้อมูล Array มาวนลูปสร้างตารางสรุป
    * ใช้ฟังก์ชัน `.toLocaleString()` เพื่อใส่ลูกน้ำ (Comma) ให้ตัวเลขราคา (เช่น 1,500)

---

## 💾 5. โครงสร้างฐานข้อมูล (Database Schema)
**ตาราง:** `runners`

| ชื่อฟิลด์ (Column) | ประเภท (Type) | คำอธิบาย |
| :--- | :--- | :--- |
| `id` | INT (Auto Increment) | รหัสประจำตัว (Primary Key) สร้างเองอัตโนมัติ |
| `name` | VARCHAR(255) | ชื่อ-นามสกุล ของผู้สมัคร |
| `distance` | VARCHAR(50) | ประเภทระยะทาง (5KM, 10KM, 21KM) |
| `price` | INT | ค่าสมัคร (เก็บเป็นตัวเลขเพื่อนำไปคำนวณต่อได้) |

---
